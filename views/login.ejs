<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-screen bg-zinc-900 text-white flex flex-col items-center justify-center">
        <h3 class="text-3xl mb-10">Login</h3>
        <form action="/login" method="post" class="flex flex-col gap-4">
            <input class="px-3 py-2 rounded-md bg-transparent border-2 border-zinc-800 outline-none" 
                   type="email" placeholder="Email" name="email" required>
            <input class="px-3 py-2 rounded-md bg-transparent border-2 border-zinc-800 outline-none" 
                   type="password" placeholder="Password" name="password" required>
            <input class="px-3 py-2 rounded-md bg-blue-500 hover:bg-blue-600 text-white cursor-pointer" 
                   type="submit" value="Login">
        </form>
        <span class="mt-5">Don't have an Account? <a class="text-blue-400" href="/">Register</a></span>
    </div>
</body>
</html>
