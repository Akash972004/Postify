<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white">

    <div class="max-w-3xl mx-auto p-6">
        
        <!-- Back Button -->
        <div class="mb-6">
            <a href="/profile" class="text-gray-400 hover:text-white transition">⬅ Back to Profile</a>
        </div>

        <!-- Edit Post Form -->
        <div class="bg-zinc-800 p-6 rounded-lg shadow-lg">
            <h1 class="text-2xl font-bold mb-4">Edit Your Post</h1>
            <form action="/edit/<%= post._id %>" method="POST">
                <textarea 
                    name="content"
                    class="resize-none bg-transparent border border-zinc-700 rounded-lg outline-none w-full h-40 p-3 text-white placeholder-gray-500 focus:border-blue-500 transition"
                    required
                ><%= post.content %></textarea>

                <div class="mt-4 flex gap-4">
                    <button 
                        type="submit"
                        class="px-4 py-2 bg-blue-500 hover:bg-blue-600 rounded-lg shadow-md transition"
                    >
                        Save Changes
                    </button>
                    <a 
                        href="/profile"
                        class="px-4 py-2 bg-gray-600 hover:bg-gray-700 rounded-lg shadow-md transition"
                    >
                        Cancel
                    </a>
                </div>
            </form>
        </div>

    </div>

</body>
</html>
